/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/CV-Portfolio",
  assetPrefix: "/CV-Portfolio/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
